POINTLINE_BOOTSTRAP_ADMIN_USERNAME=admin
POINTLINE_BOOTSTRAP_ADMIN_PASSWORD=replace-with-a-random-password-at-least-12-characters
POINTLINE_PORT=8787
# Optional: use PostgreSQL instead of the default SQLite database.
# DATABASE_URL=postgresql://pointline:password@db:5432/pointline
