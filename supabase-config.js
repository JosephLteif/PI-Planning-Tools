// Pointline uses the browser-safe Supabase publishable key. Never put a
// service_role or secret key in this file.
window.POINTLINE_SUPABASE_CONFIG = Object.freeze({
  url: '',
  publishableKey: '',
  roomId: '',
});
